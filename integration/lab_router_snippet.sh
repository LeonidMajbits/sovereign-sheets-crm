# Add this branch to the EXISTING lab command dispatcher. Do not replace ./lab.
# Assumes LAB_REPO_ROOT names the existing repository checkout.
# crm) shift; exec "$LAB_PYTHON" "$LAB_REPO_ROOT/tools/crm-lab.py" "$@" ;;
